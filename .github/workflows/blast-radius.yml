# Blast Radius – PR Analysis Workflow
#
# Prerequisites:
#   1. Set the repository secret BLAST_RADIUS_URL to the full endpoint URL,
#      e.g. https://my-blast-radius.example.com/analysis/pr
#   2. If the service enforces auth (BLAST_RADIUS_API_KEY set on the service),
#      set the repository secret BLAST_RADIUS_API_KEY to the same value.
#   3. The Blast Radius service must be running and reachable from GitHub-hosted runners.
#   4. The service returns JSON: { "overallRisk": "...", "impactAreas": [...], "suggestedTests": [...] }

name: Blast Radius Analysis

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  pull-requests: write
  contents: read

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Compute diff and call Blast Radius API
        env:
          BLAST_RADIUS_URL: ${{ secrets.BLAST_RADIUS_URL }}
          BLAST_RADIUS_API_KEY: ${{ secrets.BLAST_RADIUS_API_KEY }}
        run: |
          set -euo pipefail

          BASE_REF="origin/${{ github.event.pull_request.base.ref }}"
          HEAD_REF="HEAD"
          BASE_BRANCH="${{ github.event.pull_request.base.ref }}"
          TARGET_BRANCH="${{ github.event.pull_request.head.ref }}"

          # Generate the diff between the PR base and head
          DIFF=$(git diff "$BASE_REF"..."$HEAD_REF" || true)

          if [ -z "$DIFF" ]; then
            echo "::warning::No diff detected between $BASE_REF and $HEAD_REF"
            # Write a synthetic response so downstream steps still work
            cat > response.json <<'EMPTY'
          {"overallRisk":"UNKNOWN","impactAreas":["No diff detected between base and head — nothing to analyze."],"suggestedTests":[]}
          EMPTY
            exit 0
          fi

          # Build JSON payload using jq to safely escape the diff string
          PAYLOAD=$(jq -n \
            --arg base "$BASE_BRANCH" \
            --arg target "$TARGET_BRANCH" \
            --arg diff "$DIFF" \
            '{ baseBranch: $base, targetBranch: $target, diff: $diff }')

          # Authenticate when the service requires it (X-API-Key, see ApiKeyAuthFilter)
          CURL_AUTH=()
          if [ -n "${BLAST_RADIUS_API_KEY:-}" ]; then
            CURL_AUTH=(-H "X-API-Key: ${BLAST_RADIUS_API_KEY}")
          fi

          # Call the Blast Radius API
          HTTP_CODE=$(curl -s -o response.json -w "%{http_code}" \
            -X POST \
            -H "Content-Type: application/json" \
            "${CURL_AUTH[@]}" \
            -d "$PAYLOAD" \
            "$BLAST_RADIUS_URL")

          echo "Blast Radius API returned HTTP $HTTP_CODE"

          if [ "$HTTP_CODE" -lt 200 ] || [ "$HTTP_CODE" -ge 300 ]; then
            echo "::warning::Blast Radius API unreachable (HTTP $HTTP_CODE) — posting advisory, not failing the PR"
            cat response.json || true
            # Ephemeral dev service down (e.g. ngrok offline) → advisory comment instead of a red check.
            cat > response.json <<'UNAVAIL'
          {"overallRisk":"UNKNOWN","impactAreas":["Analysis service was unreachable — this check is advisory and does not block the PR."],"suggestedTests":[]}
          UNAVAIL
            exit 0
          fi

          # Validate that response.json is valid JSON
          if ! jq empty response.json 2>/dev/null; then
            echo "::error::Blast Radius API returned invalid JSON"
            cat response.json
            exit 1
          fi

      - name: Extract response fields
        run: |
          set -euo pipefail

          # Extract overallRisk with a fallback
          RISK=$(jq -r '.overallRisk // "UNKNOWN"' response.json)
          echo "BLAST_RISK=$RISK" >> "$GITHUB_ENV"

          # Extract impactAreas as a markdown bullet list (one per line)
          IMPACT=$(jq -r '
            if (.impactAreas // [] | length) > 0
            then .impactAreas[] | "- " + .
            else "- _None reported_"
            end
          ' response.json)

          # Write multiline value using heredoc delimiter
          {
            echo "BLAST_IMPACT<<BLAST_EOF"
            echo "$IMPACT"
            echo "BLAST_EOF"
          } >> "$GITHUB_ENV"

          # Extract suggestedTests as a markdown bullet list
          TESTS=$(jq -r '
            if (.suggestedTests // [] | length) > 0
            then .suggestedTests[] | "- " + .
            else "- _None suggested_"
            end
          ' response.json)

          {
            echo "BLAST_TESTS<<BLAST_EOF"
            echo "$TESTS"
            echo "BLAST_EOF"
          } >> "$GITHUB_ENV"

      - name: Post PR comment
        uses: actions/github-script@v7
        with:
          script: |
            const risk = process.env.BLAST_RISK || 'UNKNOWN';
            const impact = process.env.BLAST_IMPACT || '- _None reported_';
            const tests = process.env.BLAST_TESTS || '- _None suggested_';

            const riskEmoji = {
              'LOW': '🟢',
              'MEDIUM': '🟡',
              'HIGH': '🔴',
              'ERROR_UPSTREAM': '⚠️',
              'PARSING_ERROR': '⚠️',
              'UNKNOWN': '❓'
            };

            const emoji = riskEmoji[risk] || '❓';

            // Hidden marker so re-runs update the existing comment instead of stacking new ones
            const marker = '<!-- blast-radius-analysis -->';

            const body = [
              marker,
              '## 💥 Blast Radius Analysis',
              '',
              `**Overall Risk:** ${emoji} ${risk}`,
              '',
              '### 🔍 Impact Areas',
              impact,
              '',
              '### 🧪 Suggested Tests',
              tests,
              '',
              '---',
              `*Analyzed by Blast Radius (Spring Boot + Groq LLM) • ${new Date().toISOString().slice(0, 10)}*`
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100
            });

            const existing = comments.find(c => c.body && c.body.includes(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }
